import {ClipLoader} from "react-spinners";

function Loading() {

    return (
        <ClipLoader

            color={"#6184a8"}
            loading={true}
            size={150}
        />
    )
}

export default Loading